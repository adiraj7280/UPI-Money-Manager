{
  "name": "upi-money-manager",
  "version": "1.0.0",
  "description": "Automated UPI transaction categorization dashboard with live stream, smart parsing, and spending analytics",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm start",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "concurrently": "^8.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "uuid": "^9.0.0"
  }
}
